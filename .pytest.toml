# .pytest.toml
[pytest]
pythonpath = ["src"]
minversion = "9.0"
addopts = ["-ra", "-q"]
testpaths = [
    "tests",
    "integration",
]
markers = [
    "typer: tests requiring typer, for the primary CLI",
    "argparse: tests for the argparse-based fallback CLI"
]

